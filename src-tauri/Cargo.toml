[package]
name = "vocalry"
version = "0.1.0"
description = "8D Studio — AI stem separation and binaural mixing"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"

[profile.release]
codegen-units = 1
lto = true
strip = true
